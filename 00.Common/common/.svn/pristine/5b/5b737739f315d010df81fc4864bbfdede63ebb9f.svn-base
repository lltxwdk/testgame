#include "gtest/gtest.h"
#include <stdarg.h>
#include "NetClient.h"
/*
class NetClientTest : public testing::Test 
{
protected:
};

TEST_F(NetClientTest, InetAddress)
{
    InetAddress nAddr1;
    EXPECT_TRUE(nAddr1.Empty());
    InetAddress nAddr2(ntohl(inet_addr("127.0.0.1")), 80);
    EXPECT_FALSE(nAddr2.Empty());
    EXPECT_STREQ("127.0.0.1", nAddr2.ToString().c_str());
    EXPECT_STREQ("127.0.0.1:80", nAddr2.AsString().c_str());
    nAddr2.Clear();
    EXPECT_TRUE(nAddr2.Empty());
    
    InetAddress nAddr3("127.0.0.1", 80);
    EXPECT_FALSE(nAddr3.Empty());
    EXPECT_STREQ("127.0.0.1", nAddr3.ToString().c_str());
    EXPECT_STREQ("127.0.0.1:80", nAddr3.AsString().c_str());
    
    InetAddress nAddr4 = nAddr3;
    EXPECT_STREQ("127.0.0.1:80", nAddr4.AsString().c_str());
    EXPECT_FALSE(nAddr3.Empty());
}
*/

